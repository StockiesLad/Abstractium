{
  "schemaVersion": 1,
  "id": "${modid}",
  "version": "${version}",

  "name": "${mod_name}",
  "description": "A tool to provide abstracted access to a dependency. It's intended purpose is to allow the same code to work with multiple different dependencies and versions of those given dependencies. This allows a mod to theoretically work with multiple different minecraft versions and even modloaders.",
  "authors": [
    "StockiesLad [Main dev]",
    "Feltmc [Organisation]"
  ],
  "contact": {
    "sources": "https://github.com/BumbleSoftware/Fastload",
    "issues": "https://github.com/BumbleSoftware/Fastload/issues"
  },

  "license": "LGPL-3.0",
  "icon": "assets/${modid}/icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "net.feltmc.${modid}.init.AbstractiumCommon"
    ],
    "client" : [
      "net.feltmc.${modid}.init.AbstractiumClient"
    ]
  },
  "depends": {
    "fabricloader": "*",
    "minecraft": "*",
    "java": ">=17"
  }
}
